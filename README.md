# 📊 Учёт рабочих часов (с базой данных)

Приложение для отслеживания рабочих часов с сохранением данных в базе данных SQLite.

## 🚀 Установка и запуск

### 1. Установите Python 3.7 или выше
Проверьте версию:
```bash
python --version
```

### 2. Установите зависимости
```bash
pip install -r requirements.txt
```

Или установите вручную:
```bash
pip install Flask flask-cors
```

### 3. Запустите сервер
```bash
python server.py
```

### 4. Откройте браузер
Перейдите по адресу: **http://localhost:5000**

## 📁 Структура файлов

```
├── server.py                      # Серверная часть (Flask + SQLite)
├── work-hours-tracker-db.html     # Интерфейс приложения
├── requirements.txt               # Зависимости Python
├── work_hours.db                  # База данных (создаётся автоматически)
└── README.md                      # Инструкция
```

## 💾 База данных

- **Тип**: SQLite (файл `work_hours.db`)
- **Расположение**: В той же папке, что и `server.py`
- **Структура таблицы**:
  ```sql
  CREATE TABLE entries (
      id INTEGER PRIMARY KEY,
      date TEXT UNIQUE,
      hours REAL,
      created_at TIMESTAMP
  )
  ```

## 🔧 API Endpoints

- `GET /api/entries` - Получить все записи
- `POST /api/entries` - Добавить/обновить запись
- `DELETE /api/entries/<date>` - Удалить запись

## ✨ Особенности

- ✅ Данные хранятся в реальной базе данных
- ✅ Можно запустить на сервере и использовать с любого устройства
- ✅ Автоматическое обновление при изменении записей
- ✅ Индикатор подключения к БД
- ✅ Обработка ошибок и показ статуса

## 🌐 Запуск на сервере

Для доступа с других устройств в локальной сети:

1. Узнайте IP-адрес компьютера:
   - Windows: `ipconfig`
   - Linux/Mac: `ifconfig` или `ip addr`

2. Сервер уже настроен на `0.0.0.0`, поэтому будет доступен по IP

3. На других устройствах открывайте: `http://ВАШ_IP:5000`

## 🔐 Безопасность

⚠️ **Важно**: Это базовая версия для локального использования. 

Для публичного доступа добавьте:
- Аутентификацию пользователей
- HTTPS (SSL сертификат)
- Лимиты запросов
- Валидацию данных на сервере

## 📝 Примеры использования

### Добавить запись через API
```bash
curl -X POST http://localhost:5000/api/entries \
  -H "Content-Type: application/json" \
  -d '{"date": "2026-02-04", "hours": 8.5}'
```

### Получить все записи
```bash
curl http://localhost:5000/api/entries
```

### Удалить запись
```bash
curl -X DELETE http://localhost:5000/api/entries/2026-02-04
```

## 🛠️ Технологии

- **Backend**: Python + Flask
- **Database**: SQLite
- **Frontend**: HTML + JavaScript (Vanilla)
- **Styling**: Custom CSS

## 📊 Как работает

1. **Frontend** отправляет запросы к API
2. **Flask** обрабатывает запросы
3. **SQLite** сохраняет данные в файл
4. Данные возвращаются обратно в интерфейс

## 🐛 Решение проблем

**Порт 5000 занят?**
Измените порт в `server.py`:
```python
app.run(debug=True, host='0.0.0.0', port=8080)
```

**Ошибка импорта Flask?**
```bash
pip install --upgrade Flask flask-cors
```

**База данных не создаётся?**
Проверьте права на запись в папку, где находится `server.py`

---

**Автор**: Claude
**Дата**: 2026
